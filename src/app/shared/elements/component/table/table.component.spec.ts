import { TableComponent } from "./table.component";

describe('SUT: TableComponent', () => {
  let sut: TableComponent;
  beforeEach(() => {
    sut = new TableComponent();

  });

  it('should create', () => {
    // assert 
    expect(sut).toBeTruthy();
  });

  
});
